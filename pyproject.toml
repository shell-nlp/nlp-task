[project]
name = "nlp-task"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "accelerate>=1.1.1",
    "datasets>=3.1.0",
    "loguru>=0.7.2",
    "torch>=2.5.1",
    "transformers>=4.46.2",
]
